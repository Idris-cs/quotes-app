{
  "version": 2,
  "builds": [
    {
      "src": "run.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "run.py"
    }
  ],
  "installCommand": "pip install --upgrade pip && pip install -r requirements.txt",
  "buildCommand": "echo \"No build step; dependencies installed via installCommand\""
}
