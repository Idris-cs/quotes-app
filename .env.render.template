# Render Environment Configuration Template
# Copy this content to Render Dashboard → Environment Variables

# === REQUIRED: PostgreSQL Database ===
# Get from Supabase: Settings → Database → Connection String → URI
# Format: postgresql://user:password@host:port/dbname
DATABASE_URL=postgresql://postgres.YOUR_SUPABASE_USER:YOUR_PASSWORD@YOUR_HOST:6543/postgres

# === REQUIRED: Flask Secret Key ===
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# Or use Render's built-in secret generator
SECRET_KEY=your_secure_random_key_here

# === Flask Configuration ===
FLASK_ENV=production
FLASK_DEBUG=False
PORT=8000

# === Database Connection Settings ===
DB_CONNECT_RETRIES=5
DB_CONNECT_BASE_DELAY=1.0

# === Application Settings ===
FLASK_APP=run.py

# NOTE: Do NOT commit real secrets to git
# Set these values directly in Render Dashboard:
# 1. Go to your Service
# 2. Click "Environment"
# 3. Add each variable above
# 4. Click "Save"
